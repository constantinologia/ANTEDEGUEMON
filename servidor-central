require 'socket
serverSocket = TCPServer.new(2100)
puts 'Servidor iniciado
clientSocket = serverSocket.accept
puts 'Aceitando conexao
dominio = clientSocket.gets.chomp
ip = clientSocket.gets.chom
clientSocket.puts('Dominio:' + dominio + '   IP:'+ ip)
clientSocket.close
serverSocket.close
